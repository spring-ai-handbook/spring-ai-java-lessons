# 课程代码规范

- 每一课新增独立 `lessonXX` 包，不覆盖前面的课程。
- Java 类、公开方法、关键字段与复杂逻辑使用中文注释。
- 依赖注入优先 `@RequiredArgsConstructor`。
- DTO 优先使用 record，避免与课程目标无关的样板 getter/setter。
- 课程代码保持“可读优先”，避免过早抽象成复杂框架。
- 所有模型密钥通过环境变量注入，禁止提交真实凭据。
